input:
    event_start: 0
    event_num: -1

    # EventBuiltROOTData:
    #     algorithm: EventBuilder
    #     parallel: True
    Neutron:
        algorithm: ReaderROOT
        files:
            - $PYRATE/test/data/gamma_pos5_test.root
        trees:
            - nT
            - TotT
            # - eventnumber: nT/eventnumber

configs:
    - $PYRATE/config/muondet/objects.yaml
    - $PYRATE/config/muondet/analysis.yaml

outputs:
    TestROOTFile:
        path: $PYRATE/test
        format: root
        targets:
            - muonDetPlot1D

# EOF
