runs:

    WaveformAlgorithmTest:
        event_range: [0, -1]
        
        inputs:
            EventBuiltInput:
                algorithm: EventBuilder
                window: 2000
                input:
                    PSDReader:
                        reader: ReaderCAEN1730_PSD
                        files:
                            - PYRATE/test/data/1730_PSDTest-Board_0-001.dat
                

        configs:
            - PYRATE/config/MultiCHExample.yaml

        outputs:
            AlgorithmTest:
                path: PYRATE/test
                format: root
                targets:
                    - Channel_0: Input
                    # - Channel_1: Data1
                    # - Single_data: Data1
                    # - MultiChannels: Data1

# EOF